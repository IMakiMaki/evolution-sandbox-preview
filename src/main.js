const canvas=document.getElementById('worldCanvas');const ctx=canvas.getContext('2d');const tickEl=document.getElementById('tickValue');const runBtn=document.getElementById('runBtn');let running=false;let tick=0;let points=[{x:100,y:100,vx:1,vy:1},{x:200,y:200,vx:-1,vy:1},{x:300,y:150,vx:1.2,vy:-1}];function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);for(let p of points){ctx.beginPath();ctx.arc(p.x,p.y,10,0,Math.PI*2);ctx.fillStyle='cyan';ctx.fill();}}function update(){for(let p of points){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>canvas.width)p.vx*=-1;if(p.y<0||p.y>canvas.height)p.vy*=-1}}function loop(){if(!running)return;tick++;tickEl.textContent=tick;update();draw();requestAnimationFrame(loop)}runBtn.onclick=()=>{running=!running;if(running)loop()}