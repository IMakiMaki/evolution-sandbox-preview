export function drawWorld(canvas,world){const ctx=canvas.getContext('2d');ctx.clearRect(0,0,canvas.width,canvas.height);for(const s of world.species){const r=Math.max(8,s.population/5);ctx.beginPath();ctx.arc(s.x,s.y,r,0,Math.PI*2);ctx.fillStyle=s.color;ctx.fill();ctx.fillStyle='#fff';ctx.fillText(s.name,s.x-r,s.y-r-4)}}
export function drawTrend(canvas,history){const ctx=canvas.getContext('2d');ctx.clearRect(0,0,canvas.width,canvas.height);const maxPop=Math.max(...history.flatMap(h=>h.species.map(s=>s.population)),1);history.forEach((h,i)=>{h.species.forEach(s=>{const x=i*10;const y=canvas.height-(s.population/maxPop)*canvas.height;ctx.fillStyle=s.color;ctx.fillRect(x,y,4,4)})})}