/* BOIDS + GROUP VERSION */
function mulberry32(a){return function(){let t=a+=0x6D2B79F5;t=Math.imul(t^t>>>15,t|1);t^=t+Math.imul(t^t>>>7,t|61);return((t^t>>>14)>>>0)/4294967296}}
const speciesPresets=[{id:'grazer',name:'草食敏捷体',color:'#9df2b1',traits:{speed:62,camouflage:38,fertility:76,metabolism:58,resistance:42,aggression:18}},{id:'predator',name:'中型捕食体',color:'#ffb37c',traits:{speed:70,camouflage:34,fertility:32,metabolism:72,resistance:55,aggression:84}}]
function clamp(n,min,max){return Math.max(min,Math.min(max,n))}
function createAgents(r,count,x,y){return Array.from({length:count},()=>({x:x+(r()-.5)*60,y:y+(r()-.5)*60,vx:(r()-.5),vy:(r()-.5)}))}
export function createInitialWorld({seed}){const r=mulberry32(seed);const world={random:r,tick:0,environment:{temperature:50,humidity:60,resources:100},species:speciesPresets.map((p,i)=>({ ...p,population:80,x:200+i*200,y:200,agents:createAgents(r,12,200+i*200,200)})),history:[]};return world}
function boids(species){const agents=species.agents;for(let a of agents){let ax=0,ay=0,cx=0,cy=0,sx=0,sy=0,count=0;for(let b of agents){if(a===b)continue;let dx=b.x-a.x,dy=b.y-a.y,d=Math.hypot(dx,dy);if(d<80){ax+=b.vx;ay+=b.vy;cx+=b.x;cy+=b.y;count++;if(d<20){sx-=dx;sy-=dy}}}if(count){ax/=count;ay/=count;cx=cx/count-a.x;cy=cy/count-a.y;a.vx+=ax*.05+cx*.003+sx*.02;a.vy+=ay*.05+cy*.003+sy*.02}a.x+=a.vx;a.y+=a.vy;a.x=clamp(a.x,40,860);a.y=clamp(a.y,40,480)}}
export function stepWorld(world){world.tick++;const predator=world.species.find(s=>s.id==='predator');const grazer=world.species.find(s=>s.id==='grazer');boids(predator);boids(grazer);let log=[];if(predator&&grazer){let d=Math.hypot(predator.agents[0].x-grazer.agents[0].x,predator.agents[0].y-grazer.agents[0].y);if(d<60){grazer.population-=2;predator.population+=1;log.push('捕食发生')}}return{environmentNarrative:'',speciesNarratives:log}}